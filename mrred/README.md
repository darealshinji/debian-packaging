This script downloads 'Mr Red's adventure in The Missing Balls' for GNU/Linux and creates a Debian package.

Simply run `./build.sh`

To install the build-dependencies run:

    sudo apt-get install fakeroot debhelper imagemagick


