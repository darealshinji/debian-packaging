#! /bin/sh

set -e

case "$1" in
  purge)
    if [ -d /var/dt ]; then
      rm -rf /var/dt
    fi
    if [ -L /usr/dt ]; then
      rm -f /usr/dt
    fi
    if [ -L /usr/spool ]; then
      rm -f /usr/spool
    fi
  ;;
esac

## DEBHELPER ##

exit 0
