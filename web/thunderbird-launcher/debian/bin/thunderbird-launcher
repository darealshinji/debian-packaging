#!/bin/sh

tbdir="$HOME/.local/share/thunderbird-launcher"
tbbin="$tbdir/thunderbird"

if [ ! -x "$tbbin" ]; then
  rm -rf "$tbdir"
  cp -r /usr/lib/thunderbird-launcher "$HOME/.local/share"
fi

"$tbbin" $@

